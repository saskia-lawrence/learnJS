setInterval(()=>{
    console.log('ggv');
  },5000)
  // above function prints the string 'ggv' every 5 sec forever.
  //setInterval - First parameter - Callback, Second parameter - time in milliseconds (so 4000 means 4 seconds)


  //=========

  alert('hii') 
  // shows the given string in alert dialog.
  

   //=========
  
   setTimeout(()=>{
    console.log('hello');
  },5000)
  // above function prints the string 'hello' after 5 secs (for only once)
  //setTimeout - First parameter - Callback, Second parameter - time in milliseconds (so 4000 means 4 seconds)